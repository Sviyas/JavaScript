function myFunction(a){

    if (a. length % 2 == 0) {
        return a. slice(0, a. length / 2);
        }
    
    }

console.log(myFunction('viyass'));

// extract the first half string in 
// passing the argument 
