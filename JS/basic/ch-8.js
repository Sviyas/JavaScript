function myFunction(a, b, c, d, e, f) {
  return (((a + b - c) * d) / e) ** f;
}

console.log(myFunction(6, 5, 4, 3, 2, 1));

// find the values each value
// first add the a and b and sub by c
// and multiple by d
// then divide by e, and finally
